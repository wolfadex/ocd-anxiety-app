# Safety Behavior Tracker

A tool for tracking safety behaviors.

## Development

Make sure you have [mise](https://mise.jdx.dev/) installed.

Run `mise install` to install all of the dependencies, then run `just dev` and start coding!

## Production

Production builds are hosted on Netlify and use Node. Production builds assume Node is already available on your computer. Run `npm build` for a production build.